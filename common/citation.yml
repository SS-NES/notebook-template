---

citation:
    type: bool
    default: False
    help: Do you want to create a citation file for "{{ project_name }}"?

authors:
    type: str
    help: Who is the author of "{{ project_name }}"?
    default: |- 
        {{ owner }}
    when: citation